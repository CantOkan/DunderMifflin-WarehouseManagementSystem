spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/dudemif
    password: pass-123
    username: rootUser
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: db_order_management
      port: 27017
      host: localhost
      auto-index-creation: true